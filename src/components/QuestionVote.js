import React, { useEffect, Fragment } from "react";
import { connect } from "react-redux";

const QuestionVote = () => {
  return <div>QuestionVote Page</div>;
};

const mapStateToProps = () => ({});
export default connect(mapStateToProps)(QuestionVote);
